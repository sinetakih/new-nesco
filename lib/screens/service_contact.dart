import 'package:flutter/material.dart';

class ServiceContact extends StatelessWidget {
  const ServiceContact({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
